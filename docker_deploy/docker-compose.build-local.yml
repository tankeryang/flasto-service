version: '3'

services:
  flasto-service:
    build:
      context: ../
      dockerfile: Dockerfile
    image: flasto-service:1.0.1
    container_name: flasto-service
    ports:
      - "5678:5678"
    volumes:
      - ../query_service/log:/opt/flasto-service/query_service/log
      - /etc/timezone:/etc/timezone
      - /etc/localtime:/etc/localtime
